services:
  - type: web
    name: kopyahali
    env: node
    plan: free
    buildCommand: npm install && npm run check
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: PORT
        value: 8000
